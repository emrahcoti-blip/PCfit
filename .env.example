# Server-side only — set this in Vercel > Settings > Environment Variables
ANTHROPIC_API_KEY=sk-ant-...

# Your Amazon Associates tracking tag (e.g. pcfit0c-20). Leave empty until approved —
# links will still work as plain Amazon search URLs without it.
NEXT_PUBLIC_AMAZON_TAG=
